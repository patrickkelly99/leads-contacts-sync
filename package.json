{
  "name": "leads-contacts-sync",
  "version": "1.0.0",
  "description": "Recurring sync of contact data into monday.com Leads board",
  "main": "sync.js",
  "scripts": {
    "start": "node sync.js",
    "dev": "node --watch sync.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "node-cron": "^3.0.3"
  }
}
